# Local environment variables (EXAMPLE)
# Do NOT commit this file. Add to .gitignore if not already ignored.

# ALL EXTERNAL API INTEGRATIONS DISABLED
# X (Twitter) API v2 bearer token (server-side only)
# X_BEARER_TOKEN=

# X username to fetch timeline from (without @)
# X_USERNAME=

# Cache TTL in seconds for timeline endpoint (optional, default 300)
# X_CACHE_TTL_SECONDS=

# Optional: other service tokens (examples)
# INSTAGRAM_ACCESS_TOKEN=
# INSTAGRAM_USER_ID=
# NOTE: keep all secrets server-side and do NOT prefix with VITE_ (or they will be exposed to client)

# VITE_ENABLE_X_FEED=
# VITE_ENABLE_INSTAGRAM_FEED=